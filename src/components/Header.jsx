import React from 'react';

const Header = () => {
  return (
    <header>
      <div className="container">
        <h1>Erim Uludağ</h1>
        <div className="header-right">
          {/* Removed generic navbar - using only the button-triggered sidebar navigation */}
        </div>
      </div>
    </header>
  );
};

export default Header;